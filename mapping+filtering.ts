/**
 * Use this array of numbers if you want to
 * manually test your functions
 */
const numbers = [10, 13, 20, 25, 38, 35, 40];

// Using `filteredAndSquared` function return the numbers that are greater than or equal to 20 and then square each of them.
function filteredAndSquared(numbers) {
  // write your code here...
}

// Using `filteredAndTripled` return the numbers that are divisible by 5 and then multiply each of them by 3.
function filteredAndTripled(numbers) {
  // write your code here...
}

export { filteredAndSquared, filteredAndTripled };
