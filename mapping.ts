/**
 * Use this array of numbers if you want to
 * manually test your functions
 */
const numbers = [10, 13, 20, 25, 38, 35, 40];

// Using `squaredNumbers` function return a new array that contains each number squared.
function squaredNumbers(numbers) {
  // write your code here...
}

// Using `doubledNumbers` function return a new array that contains each number multiplied by 2.
function doubledNumbers(numbers) {
  // write your code here...
}

export { squaredNumbers, doubledNumbers };
